# Architecture Rules

**데이터와 UI의 분리**
- 데이터 구조를 UI 코드와 직접 강하게 결합하지 않는다.
- 상태 변경과 DOM 렌더링의 책임을 분리한다.

**의존성 관리**
- 새로운 기능 추가 전에 기존 기능과의 의존성을 먼저 파악한다.
- 동일한 로직을 여러 파일에 복제하지 않는다.

**파일 책임**
- 파일 하나가 지나치게 많은 책임을 가지지 않도록 한다.
- 명확한 목적을 가진 모듈로 구성한다 (데이터 로드, 필터링, 렌더링, Insight 등).
